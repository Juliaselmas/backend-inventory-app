/** @type {import('postcss-load-config').Config} */
const config = {
    plugins: {
      // Här kan du lägga till andra PostCSS-plugins om du använder dem
      // Exempel:
      // autoprefixer: {},
    },
  };
  
  export default config;